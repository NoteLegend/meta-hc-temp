name: sqlite-rescue-env
version: "1.0.0"
description: "A financial auditing environment where an AI agent must inspect, filter, categorise, and flag anomalies in a SQLite database."
author: "Data Engineering Hackathon Participant"

build:
  dockerfile: Dockerfile

tasks:
  - finaudit_easy
  - finaudit_medium
  - finaudit_complex